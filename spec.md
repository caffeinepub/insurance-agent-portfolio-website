# Specification

## Summary
**Goal:** Change the footer's bottom bar (copyright bar) text color to white.

**Planned changes:**
- In `HoustonFooter.tsx` and `Footer.tsx`, update the 4th child div's inner div in the footer container to use `text-white` for its text color.

**User-visible outcome:** The copyright/bottom bar text in the footer displays in white.
